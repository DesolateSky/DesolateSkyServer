package net.desolatesky.structure;

import net.desolatesky.instance.DSInstance;
import net.minestom.server.coordinate.Point;

public interface Structure {

    void place(DSInstance instance, Point origin);

}
